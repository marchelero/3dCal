# Session: F1-F2-F3 completos

**Date**: 2026-07-17
**Status**: 130/130 tests pass. Build 0 errores.

---

## F1 — Mano de obra + Post-procesado ✅

**Engine**: `CalculationEngine.compute()` implementa formula completa:

```
materialCost + electricCost + laborCost + postProcessCost = baseCost
baseCost + failureCost + markupCost + profitAmount = total
total - discount = totalPrice
```

**Nueva seccion OTROS** colapsable en calculator (Express + Advanced), debajo de Descuento:
- Mano de obra (Bs/h)
- Post-procesado (% de materiales)
- Tasa de falla (% del base)
- Desperdicio (% markup)

Campos en grid 2×2 dentro de card colapsable (`_showOtros` local state).

**DB migration** v1/v2 → v3: 11 columnas nuevas en `calculations` (laborCostSnapshot, laborRateSnapshot, etc.).

**DetailSection** (ojito) muestra desglose completo: electricidad, labor, post-procesado, base, falla, markup, profit.

## F2 — Export PDF/CSV + Search/Filter ✅

### Historial search/filter
- SearchBar con texto + clear button en calculations list page
- 3 FilterChips: Todas / Vendidas / Pendientes (Wrap, evita overflow)
- `CalculationsNotifier` re-escrito: cache `_all`, in-memory filter por query + sold status
- EmptyView con "Sin resultados" cuando search activo sin matches

### CSV export
- Boton `file_download` en AppBar del historial
- Genera CSV con columnas: Fecha, Pieza, Cliente, Total, Vendido, CostoMat, Electricidad, Profit, Materiales, Horas, Descuento
- Comparte via `share_plus` `XFile.fromData()`

### PDF export
- `lib/core/export/pdf_export.dart` con `shareQuotePdf()`
- Usa `pdf` package (Dart PDF vectorial)
- Roboto TTF fonts registradas (assets/fonts/) — soporta ñ, acentos, Unicode
- `XFile.fromData()` sin disk write — compatible web
- Boton PDF (icono picture_as_pdf, rojo) en result sheet + detail page

## F3 — Dashboard v2 ✅

### Monthly trend chart
- `MonthlyTotal` model + `monthlyTotals()` repo method con SQL nativo:
  ```sql
  SELECT strftime('%Y-%m', created_at) AS month,
         COALESCE(SUM(...), 0) AS quoted, ...
  ```
- `MonthlyTrendChart` widget con fl_chart LineChart
- 2 lineas: cotizado (curva suave, area fill primary) + vendido (curva suave, tertiary)
- Eje X: labels de mes rotados 45°
- Min 2 puntos de datos para mostrar

### Top materials
- `TopMaterial` model + `topMaterials()` repo method con `GROUP BY label`
- Lista de materiales mas usados con count + peso total
- Formato: gramos (g) / kilogramos (kg) segun magnitud

### Layout dashboard:
1. Stats row (3 StatTiles: Cotizaciones, Vendidas, Conversion %)
2. Card money row (Total cotizado / Total vendido)
3. ProfitBarChart (barras cotizado vs vendido)
4. MonthlyTrendChart (solo si >= 2 meses datos)
5. Top materials card (solo si hay datos)

### Bugfixes:
- SQL COALESCE + null-safe reads para DB vacia
- `Variable<int>` en LIMIT (no Variable<double>)
- Settings test: uppercase section headers (`_SettingsSection` usa `title.toUpperCase()`)

---

## F4 — Multi-moneda USD (pendiente)

**Proximo paso**: toggle BOB/USD en settings + tipo de cambio manual.

Archivos clave a modificar:
- `lib/l10n/es_bo.dart` — strings para moneda
- `lib/core/constants/app_constants.dart` — `kCurrencyCode`, `kCurrencySymbol`
- `lib/features/settings/domain/settings.dart` — agregar currency y exchangeRate
- `lib/features/settings/presentation/pages/settings_page.dart` — UI toggle
- `lib/core/money/currency_formatter.dart` — logica dual BOB/USD
- Todas las UI que muestran precios

## F5 — Catalog search (pendiente)

Search bar en dialogos de filamento/impresora.

## F6 — Multi-idioma EN (pendiente)

Sistema de localizacion propio con archivos en/es.

## F7 — Print quote + Onboarding (pendiente)

Web print stylesheet + 4-pantallas onboarding.
