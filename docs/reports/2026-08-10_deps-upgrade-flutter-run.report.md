# Fix flutter run + Upgrade de dependencias — Report de ejecucion

> Generated by primary (build) on 2026-08-10 from issue: `flutter run -d chrome` fallaba.

## Status
COMPLETADO

## Contexto
`flutter run -d chrome` fallaba. Dos causas raiz:
1. `flutter` no estaba en PATH (`.zshrc` apuntaba a `/home/marcelo/dev/flutter/bin` que no existia; el SDK real estaba en `/home/marcelo/dev/otro/flutter` v3.44.1). El usuario lo copio de vuelta a `/home/marcelo/dev/flutter/bin` y con `source ~/.zshrc` el PATH quedo OK.
2. Las dependencias estaban muy desactualizadas (Riverpod 2.x, go_router 14, fl_chart 0.68, drift 2.28, custom_lint...), con conflictos de ecosistema que impedian resolver a un set consistente. Se hizo upgrade a las majors actuales con pins especificos (ver Decisiones).

## Agentes usados
Sin dispatch de sub-agentes: fix directo del primary (analyze/test/build como verificacion).

| Fase | Agente | Output | Status |
|------|--------|--------|--------|
| 0 | primary (build) | Report | OK |

## Decisiones tomadas
- **PATH de Flutter**: usar `/home/marcelo/dev/flutter/bin` (ya copiado por el usuario) + `source ~/.zshrc`. Quedo un duplicado del SDK en `~/dev/otro/flutter` (candidato a borrar).
- **Set de versiones pinneado por el ecosistema** (eslabon principal de la cadena de conflictos):
  - Flutter SDK 3.44.1 pinnea `meta 1.18.0` y `test_api 0.7.11` → `test` maximo `1.31.0` (test_api 0.7.11).
  - `test 1.31.0` exige analyzer `>=8 <13` → analyzer 12 como techo.
  - `riverpod_lint` → `3.1.4` (analyzer ^12, riverpod 3.3.2 exacto).
  - `drift_dev` → `2.34.0` (analyzer >=10 <13); `riverpod_generator` → `4.0.4` (analyzer ^12).
  - `build_runner` → `2.15.1` (pin; 2.16 exige analyzer >=13.3).
  - `custom_lint` **eliminado**: riverpod_lint 3.x ya no lo usa (es analysis_server_plugin).
  - `dependency_overrides: test: 1.31.0`.
- **Riverpod 2→3**: `valueOrNull` → `value` (masivo); `StateNotifier`/`StateProvider` viven en `flutter_riverpod/legacy.dart`; `Override`/`ProviderOverride` ya no son tipos exportados → dejar inferencia.
- **fl_chart 1.2**: `SideTitleWidget` cambio `axisSide:` → `meta:`.
- **analysis_options.yaml**: plugin `custom_lint` → `riverpod_lint: 3.1.8` (top-level).

## Criterios de exito

| # | Criterio | Estado | Evidencia |
|---|----------|--------|-----------|
| 1 | `flutter analyze` sin errores | PASS | 0 errores (post-upgrade paso de 72 errores a 0) |
| 2 | `flutter test` no peor que baseline | PASS | +323 -9 (baseline +316 -10; los 9 fallos restantes son pre-existentes, `No ProviderScope found` en result_sheet.dart:636) |
| 3 | `flutter build web --release` OK | PASS | `Built build/web` (125s) |
| 4 | `flutter pub get` resuelve | PASS | 53 dependencias cambiadas |

## Desvios / Incidentes
- **10 tests fallando pre-existentes** (ahora 9): `result_sheet_test.dart` y `quote_save_flow_test.dart` — ya fallaban ANTES del upgrade (baseline +316 -10). Causa: `No ProviderScope found` en result_sheet.dart:636 y error de save → AppSnackBar. Deuda pre-existente, NO introducida por esta migracion.
- `--delete-conflicting-outputs` de build_runner ya no existe en 2.15 (flag ignorado, no fue necesario).
- 37 dependencias del lock quedaron con versiones nuevas incompatibles por constraints (esperado, resuelto por el set pinneado).

## Skills cargadas
Ninguna (fix directo con analyze/test/build como loop de verificacion).

## Archivos modificados
- `pubspec.yaml` / `pubspec.lock` — set de dependencias actualizado + pins (ver Decisiones).
- `analysis_options.yaml` — plugin riverpod_lint reemplaza a custom_lint.
- `lib/main.dart` — removed `<Override>` de overrides (main.dart:38).
- `lib/core/theme_mode_provider.dart` — import legacy.dart (StateNotifier/StateNotifierProvider).
- `lib/core/providers.dart` — import legacy.dart (StateProvider).
- `lib/features/dashboard/presentation/widgets/profit_bar_chart.dart` — fl_chart 1.2 `meta:` (2 ocurrencias).
- `test/integration/history_cap_test.dart` — removed `<ProviderOverride>` (line 63).
- Reemplazo masivo `valueOrNull` → `value` en 20 archivos: lib/features/calculator/presentation/widgets/brand_selector_field.dart, lib/features/settings/presentation/pages/initial_config_page.dart, lib/features/calculator/application/calculator_notifier.dart, lib/features/calculations/presentation/pages/calculation_detail_page.dart, lib/features/home/presentation/pages/home_page.dart, lib/features/calculator/presentation/pages/calculator_page.dart, lib/features/calculations/presentation/pages/calculations_list_page.dart, lib/features/calculator/presentation/widgets/result_sheet.dart, lib/features/purchases/entitlement_providers.dart, lib/features/settings/presentation/pages/settings_page.dart, lib/features/settings/application/settings_notifier.dart, lib/features/filaments/application/filaments_notifier.dart, lib/features/printers/application/printers_notifier.dart, lib/features/settings/application/currency_settings_provider.dart, lib/core/providers.dart, + 5 tests. (3 menciones restantes de `valueOrNull` son comentarios).
- Codigo generado por build_runner (247 outputs: drift_dev + riverpod_generator).

## Proximos pasos
- (Opcional) Borrar SDK duplicado en `~/dev/otro/flutter`.
- (Deuda) Investigar los 9 tests fallando pre-existentes en result_sheet.dart:636 (ProviderScope no envuelve el widget bajo test).
- El usuario decide si commitea los cambios (no se commiteo sin permiso explicito).

---

*Auditable via `/audit-report {name}` → `docs/audits/{name}.audit.md`.*
