/// Stub por defecto (mobile/desktop): la Web Share API no aplica, el share
/// nativo del SO soporta archivos. Esta funcion solo se usa en web via
/// conditional import (web_share_support_web.dart).
bool webShareFilesAvailable() => true;
